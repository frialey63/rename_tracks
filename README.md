# rename_tracks
A simple bash script to simplfy the renaming of tracks for a ripped unknown album.
Because sometimes an album may be unknown to CDDB ;-(

Usage

- Locate the script on your PATH
- Create the TXT file in the directory where the tracks are located
- While located in the directory containing the tracks, execute the command `rename_tracks.sh <names.txt>`
